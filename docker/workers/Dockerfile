# Workers Dockerfile
# Celery workers with Playwright for crawling

# TODO: Implement when worker code is ready
# Base image: python:3.11-slim
# Install: pip install -r requirements/prod.txt
# Install: playwright install chromium
# Run: celery -A workers.celery_app worker --loglevel=info
