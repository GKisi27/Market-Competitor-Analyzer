# Backend Dockerfile
# FastAPI application with Uvicorn

# TODO: Implement when backend code is ready
# Base image: python:3.11-slim
# Install: pip install -r requirements/prod.txt
# Run: uvicorn app.main:app --host 0.0.0.0 --port 8000
