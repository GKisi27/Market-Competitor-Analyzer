# Production Environment Configuration
# Branch: main (prod)

# ======================
# Application
# ======================
APP_NAME=market-competitor-analyzer
APP_ENV=production
DEBUG=false
LOG_LEVEL=WARNING
SECRET_KEY=# USE SECURE SECRET FROM AWS SECRETS MANAGER

# ======================
# Backend API
# ======================
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
BACKEND_RELOAD=false
BACKEND_WORKERS=4

# ======================
# Database (uncomment based on choice)
# ======================
# PostgreSQL (AWS RDS recommended)
DATABASE_URL=# USE AWS RDS CONNECTION STRING

# MongoDB (AWS DocumentDB or MongoDB Atlas)
# MONGODB_URI=# USE SECURE CONNECTION STRING

# ======================
# Redis (AWS ElastiCache recommended)
# ======================
REDIS_URL=# USE AWS ELASTICACHE URL
CELERY_BROKER_URL=# USE AWS ELASTICACHE URL
CELERY_RESULT_BACKEND=# USE AWS ELASTICACHE URL

# ======================
# Authentication
# ======================
JWT_SECRET_KEY=# USE AWS SECRETS MANAGER
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ======================
# Frontend
# ======================
NEXT_PUBLIC_API_URL=https://api.yourcompany.com/api/v1
NEXT_PUBLIC_ENV=production

# ======================
# Crawler
# ======================
CRAWLER_DELAY_MIN=2.5
CRAWLER_DELAY_MAX=5.0
CRAWLER_HEADLESS=true
PROXY_URL=# USE ROTATING RESIDENTIAL PROXY

# ======================
# CORS (production - strict)
# ======================
CORS_ORIGINS=https://yourcompany.com,https://www.yourcompany.com

# ======================
# AWS (Production)
# ======================
AWS_REGION=us-east-1
AWS_S3_BUCKET=mca-production-bucket
# Credentials via IAM Role (recommended) or environment
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# ======================
# Monitoring (optional)
# ======================
# SENTRY_DSN=
# DATADOG_API_KEY=
